{
  "name": "micro-frontend-rsc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "micro-app"
  ],
  "scripts": {
    "dev": "npm run dev:micro-app",
    "dev:micro-app": "cd micro-app && npm run dev",
    "build": "npm run build:micro-app",
    "build:micro-app": "cd micro-app && npm run build",
    "start": "cd micro-app && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.1.6"
  }
}
